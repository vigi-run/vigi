MODE=dev
PORT=8080
DB_TYPE=sqlite
DB_NAME=./vigi.db
JWT_SECRET=dev-secret-change-in-production
JWT_EXPIRATION=24h
REDIS_HOST=localhost
REDIS_PORT=6379
CLIENT_URL=http://localhost:5173

# Storage Configuration (S3)
# To enable S3 storage, uncomment and fill in the following variables:
# S3_ENDPOINT=https://s3.amazonaws.com # or your MinIO/S3 compatible endpoint
# S3_ACCESS_KEY=your-access-key
# S3_SECRET_KEY=your-secret-key
# S3_BUCKET=your-bucket-name
# S3_REGION=us-east-1
# S3_DISABLE_SSL=false # Set to true if using HTTP instead of HTTPS (e.g. local MinIO)
